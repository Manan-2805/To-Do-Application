# TEMPLATE ONLY — Do not apply this file directly.
# This Secret is created dynamically by .github/workflows/deploy.yml
# using GitHub Secrets values via:
#   kubectl create secret generic todosphere-secrets \
#     --namespace=todosphere \
#     --from-literal=DB_PASSWORD=... \
#     --from-literal=DATABASE_URL=... \
#     --from-literal=JWT_ACCESS_SECRET=... \
#     --from-literal=JWT_REFRESH_SECRET=... \
#     --from-literal=S3_ACCESS_KEY=... \
#     --from-literal=S3_SECRET_KEY=... \
#     --from-literal=CORS_ORIGINS=... \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# Never commit real values here. Never run: kubectl apply -f k8s/secrets.yml

apiVersion: v1
kind: Secret
metadata:
  name: todosphere-secrets
  namespace: todosphere
  labels:
    app.kubernetes.io/part-of: todosphere
type: Opaque
stringData:
  DB_PASSWORD: "<set-by-deploy-workflow>"
  DATABASE_URL: "postgresql+asyncpg://postgres:<DB_PASSWORD>@postgres.todosphere.svc.cluster.local:5432/todosphere"
  JWT_ACCESS_SECRET: "<set-by-deploy-workflow>"
  JWT_REFRESH_SECRET: "<set-by-deploy-workflow>"
  S3_ACCESS_KEY: "test"
  S3_SECRET_KEY: "test"
  CORS_ORIGINS: "<set-by-deploy-workflow>"
