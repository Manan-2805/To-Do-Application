apiVersion: v1
kind: Service
metadata:
  name: localstack
  namespace: todosphere
  labels:
    app: localstack
    app.kubernetes.io/part-of: todosphere
spec:
  selector:
    app: localstack
  ports:
    - name: gateway
      port: 4566
      targetPort: 4566
