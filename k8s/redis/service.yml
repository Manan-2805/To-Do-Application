apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: todosphere
  labels:
    app: redis
    app.kubernetes.io/part-of: todosphere
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
