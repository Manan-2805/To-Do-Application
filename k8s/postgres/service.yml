apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: todosphere
  labels:
    app: postgres
    app.kubernetes.io/part-of: todosphere
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
